conda create --name Env_gpu python=3.8
conda activate Env_gpu

conda install -c conda-forge cudatoolkit=11.2 cudnn=8.1.0 

conda install pip
pip install "tensorflow==2.9"


conda install:
 
numpy=1.24.3
pandas
scipy
matplotlib
scikit-learn=1.3.0


#To use Tensorflow with GPU support, an NVIDIA GPU is required. If no NVIDA GPU is available or the experiments are to be executed on a CPU, the execution can be changed from GPU to CPU in Main_script.py.